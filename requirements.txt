discord.py>=2.3,<3
aiohttp>=3.9
python-dotenv>=1.0
tzdata>=2024.1  # ZoneInfo("Australia/Brisbane") on images without system tz data

# Companion voting web app (web.py)
Flask>=3.0,<4
Authlib>=1.3          # Discord OAuth2 login
requests>=2.31        # used by Authlib for the token exchange / API calls
gunicorn>=21.2        # production WSGI server (see docker-compose web service)
